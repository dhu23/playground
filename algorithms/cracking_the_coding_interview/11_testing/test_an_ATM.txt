# how would you test an ATM in a distributed banking system

# where, who, what, when, how, why
# the usage case:
# debt card holders use the ATM machines in the vestibule next to the street
# use it to withdraw money, deposit check or cash, view balance

# consider the system is distributed, it should be able to handle large amount
# of usages at the same time. Is the distributed banking system (software) also
# part of the test?

# physically test the ATM:
# 1) does it recognize the card or reject a card through loggin system
# 2) does it read balances for different type of accounts
# 3) once done using, does it exit correctly
# 4) can you withdraw $1, $10, $100, $1000, $10000 from it, when the account
#    has $1, $10, $100, $1000, $10000 in it?
# 5) test depositing $1 to $10000 to the account? when the account is empty?
# 6) considering distributed system, does it reflect the operation on time?
#    can you see the result immediately?
# 7) test with 1, 10, 100, 1000 users (simulated) at the same time.
